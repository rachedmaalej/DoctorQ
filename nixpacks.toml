[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@9.15.9", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @doctorq/api build"]

[start]
cmd = "pnpm --filter @doctorq/api start"
